spring:
  datasource:
    url: jdbc:mysql://localhost:33062/pocopi_test?useSSL=false&allowPublicKeyRetrieval=true
    username: pocopi_test_user
    password: test_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    org.hibernate: ERROR
    org.flywaydb.core: ERROR
    com.pocopi.api: INFO
