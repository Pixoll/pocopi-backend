services:
  mysql:
    env_file: ".env"
    image: "mysql:latest"
    ports:
      - "33061:3306"
  nginx:
    image: nginx:alpine
    ports:
      - "8081:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./images:/var/www/images